query getChatMessages($pagination: Pagination!) {
  getChatMessages(pagination: $pagination){
    messages{
      _id
      text
      sender
      date
    }
    nextPagination{
      pageNumber
      nPerPage
      nextPageNumber
    }
  }
}