mutation editFasting($id: String!, $fasting: FastingUpdateInput!) {
  editFasting(id: $id, fasting: $fasting) {
    _id
    name
    startDate
    endDate
    color
    initialTotalHours
    finished
  }
}
